# net_speed_graphs
script for creating graphs from speedtest-cli

I have a cron job that runs lightly modified version of speedtest-cli.py every hour and graph.py parses the results and creates graphs. a nother work in progress
